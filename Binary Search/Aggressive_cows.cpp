#include <iostream>
#include <vector>
using namespace std;
bool ifPossible(vector<int> stalls , int k , int mid){
    int count = 1;
    int lastPos = stalls[0];
    for(int i = 0 ; i < stalls.size() ; i++){
        if(arr[])
    }
}